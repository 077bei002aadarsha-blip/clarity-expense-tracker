# Database Configuration (Local Development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=clarity_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# OR use DATABASE_URL format (required for Railway deployment)
# DATABASE_URL=postgresql://username:password@host:port/database
# Example Railway: postgresql://postgres:password@containers-us-west-123.railway.app:5432/railway

# JWT Secret Key (use a strong random string in production)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development

# Production Note:
# Railway automatically provides DATABASE_URL environment variable when PostgreSQL is added
# Make sure to set JWT_SECRET in Railway dashboard